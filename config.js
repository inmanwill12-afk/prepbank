// Fill these in after creating your free Supabase project:
// Supabase dashboard -> Project Settings -> API.
// Both values below are PUBLIC by design (the "anon" key only ever does what
// your Row Level Security policies in supabase/schema.sql allow) and are safe
// to ship in client-side code. Never put your Anthropic API key here --
// that one stays server-side only, in Vercel's environment variables.

window.PREPBANK_CONFIG = {
  SUPABASE_URL: "https://zignzktrjsiklwepuhom.supabase.co",
  SUPABASE_ANON_KEY: "PASTE-YOUR-PUBLISHABLE-KEY-HERE",
};
